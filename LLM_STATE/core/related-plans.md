# Related Plans

## Parents
Peer projects this plan depends on:
- {{DEV_ROOT}}/TestAnyware — VM driver / agent / vision pipeline consumed by the testanyware-sdk

## Consumers
- {{DEV_ROOT}}/APIAnyware-MacOS — hosts per-app scenario suites
  (`knowledge/apps/<app>/scenarios/`, in `#lang app-spec`) and per-impl
  `--impl` config files (e.g.
  `generation/targets/racket-oo/apps/modaliser/modaliser-impl.rkt`,
  in `#lang app-spec/impl`).
